{
  "name": "friendzones-group-unified",
  "version": "2.4.1",
  "private": true,
  "description": "FriendZones public website with restaurant and lodging media delivered from Cloudflare R2 while preserving the approved Venus and Volga designs and the internal operations app.",
  "scripts": {
    "start": "vercel dev",
    "lint": "node -c api/[...path].js && node -c api_src/router.js && node -c api_src/notify.js && node -c api_src/fnb-ai.js && node -c api_src/kiotviet.js && node -c api_src/supabase.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "vercel": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
